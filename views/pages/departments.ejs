<h2 class="text-center">Departments</h2>
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% departments.forEach(department => { %>
    <tr>
      <th><%= department.id %></th>
      <td><%= department.name %></td>
      <td>
        <div class="d-flex">
          <a
            href="/departments/edit/<%= departments.id %>"
            class="btn btn-sm btn-primary m-1"
          >
            Edit
          </a>

          <form
            action="/api/departments/<%= departments.id %>?method=DELETE"
            method="post"
          >
            <button type="submit" class="btn btn-sm btn-danger m-1">
              Delete
            </button>
          </form>
        </div>
      </td>
    </tr>
    <% }) %>
  </tbody>
</table>

<div class="d-grid">
  <button class="btn btn-warning btn-sm">Create new department</button>
</div>
